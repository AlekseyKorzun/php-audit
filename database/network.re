fopen|fsockopen|file\(|file \(|popen|file_get_contents|readfile|curl_exec\(|curl_exec \(|include\('http|include \('http|include\('ftp|include \('ftp|include_once\('http|include_once \('http|include_once\('ftp|include_once \('ftp|require\('http|require \('http|require\('ftp|require \('ftp|require_once\('http|require_once \('http|require_once\('ftp|require_once \('ftp|mail \(|mail\(
